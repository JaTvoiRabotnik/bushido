# Agent Rules

This file is updated when running `tessl install`. If a linked file is missing, make sure to run the command to download any missing tiles from the registry.

## tessl/cli-setup — get_library_context

@tiles/tessl/cli-setup/steering/get_library_context.md [get_library_context](tiles/tessl/cli-setup/steering/get_library_context.md)